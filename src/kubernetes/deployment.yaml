# Source: external-dns/templates/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: vendor-external-dns-aws-sync-public
  namespace: ${Environment}
  labels:
    app.kubernetes.io/name: vendor-external-dns-aws-sync-public
    app.kubernetes.io/instance: vendor-external-dns-aws-sync-public
    app.kubernetes.io/version: "${Version}"
    app.kubernetes.io/managed-by: Kaptain
    app: vendor-external-dns-aws-sync-public
    app.kubernetes.io/part-of: ${ProductName}
    kaptain.org/environment: ${Environment}
    kaptain.org/product: ${ProductName}
    kaptain.org/project-name: ${ProjectName}
    kaptain.org/version: "${Version}"
    kaptain.org/owner: kube-kaptain
  annotations:
    kaptain.org/helm-upstream-chart: external-dns-1.21.1
    kaptain.org/generated-by: Generated by Kaptain vendor-helm-render-and-process
    kaptain.org/source-repository: kube-kaptain/vendor-external-dns-aws-sync-public
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: ${ProjectName}
      app.kubernetes.io/instance: vendor-external-dns-aws-sync-public
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app.kubernetes.io/name: ${ProjectName}
        app.kubernetes.io/instance: vendor-external-dns-aws-sync-public
    spec:
      automountServiceAccountToken: true
      serviceAccountName: vendor-external-dns-aws-sync-public
      securityContext:
        fsGroup: 65534
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: external-dns
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            privileged: false
            readOnlyRootFilesystem: true
            runAsGroup: 65532
            runAsNonRoot: true
            runAsUser: 65532
          image: ${VendorExternalDnsAwsSyncPublic/EnvironmentDockerRegistryAndNamespace}/${DockerImageName}:${DockerTagExternalDns}
          imagePullPolicy: IfNotPresent
          args:
            - --log-level=${VendorExternalDnsAwsSyncPublic/LogLevel}
            - --log-format=${VendorExternalDnsAwsSyncPublic/LogFormat}
            - --interval=${VendorExternalDnsAwsSyncPublic/Interval}
            - --source=service
            - --source=ingress
            - --source=crd
            - --policy=sync
            - --registry=txt
            - --txt-owner-id=${ProjectName}
            - --managed-record-types=A
            - --managed-record-types=AAAA
            - --managed-record-types=CNAME
            - --managed-record-types=MX
            - --managed-record-types=NS
            - --managed-record-types=PTR
            - --managed-record-types=SRV
            - --managed-record-types=TXT
            - --provider=aws
            - --aws-zone-type=public
            - --txt-prefix=_${ProjectName}.
            - ${VendorExternalDnsAwsSyncPublic/EventsFlag}
            - --min-event-sync-interval=${VendorExternalDnsAwsSyncPublic/MinEventSyncInterval}
            - --aws-zones-cache-duration=${VendorExternalDnsAwsSyncPublic/AwsZonesCacheDuration}
            - --aws-evaluate-target-health
            - --min-ttl=${VendorExternalDnsAwsSyncPublic/MinTtl}
            - --namespace=${VendorExternalDnsAwsSyncPublic/Namespace}
            - --annotation-filter=${VendorExternalDnsAwsSyncPublic/AnnotationFilter}
          ports:
            - name: http
              protocol: TCP
              containerPort: 7979
          livenessProbe:
            failureThreshold: ${VendorExternalDnsAwsSyncPublic/ProbeLivenessFailureThreshold}
            httpGet:
              path: /healthz
              port: http
            periodSeconds: ${VendorExternalDnsAwsSyncPublic/ProbeLivenessPeriod}
            timeoutSeconds: ${VendorExternalDnsAwsSyncPublic/ProbeTimeoutSeconds}
          readinessProbe:
            failureThreshold: ${VendorExternalDnsAwsSyncPublic/ProbeReadinessFailureThreshold}
            httpGet:
              path: /healthz
              port: http
            periodSeconds: ${VendorExternalDnsAwsSyncPublic/ProbeReadinessPeriod}
            successThreshold: ${VendorExternalDnsAwsSyncPublic/ProbeReadinessSuccessThreshold}
            timeoutSeconds: ${VendorExternalDnsAwsSyncPublic/ProbeTimeoutSeconds}
          startupProbe:
            httpGet:
              path: /healthz
              port: http
            periodSeconds: ${VendorExternalDnsAwsSyncPublic/ProbeStartupPeriod}
            timeoutSeconds: ${VendorExternalDnsAwsSyncPublic/ProbeTimeoutSeconds}
            failureThreshold: ${VendorExternalDnsAwsSyncPublic/ProbeStartupFailureThreshold}
          resources:
            requests:
              cpu: "${VendorExternalDnsAwsSyncPublic/CpuRequest}"
              memory: "${VendorExternalDnsAwsSyncPublic/Memory}"
            limits:
              memory: "${VendorExternalDnsAwsSyncPublic/Memory}"
          env:
            - name: GOMEMLIMIT
              value: ${VendorExternalDnsAwsSyncPublic/GoMemLimit}
